{
  "name": "dsh-plugin-provider-disable",
  "version": "0.1.0",
  "private": true,
  "description": "Disable an entire provider (model set) without deleting it: requests to disabled providers are rejected, and their models are grayed out in the model picker",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./client": {
      "default": "./lib/client.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "test": "node test/state.test.mjs && node test/reject.test.mjs && node test/client.test.mjs"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  }
}
